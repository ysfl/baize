interface:
  display_name: "Baize AI 接入"
  short_description: "通过 MCP 让 AI 查询并安全处理白泽节点状态"
  default_prompt: "Use $baize-ai to help me use Baize through the available MCP tools."
dependencies:
  tools:
    - type: "mcp"
      value: "baize"
      description: "Baize MCP connector"
      transport: "stdio"
policy:
  allow_implicit_invocation: true
