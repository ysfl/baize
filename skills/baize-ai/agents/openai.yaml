interface:
  display_name: "Baize AI 接入"
  short_description: "通过白泽 API 或 MCP 让 AI 查询并安全处理远程任务"
  default_prompt: "Use $baize-ai to help me use Baize through the available API or MCP tools."
dependencies:
  tools:
    - type: "mcp"
      value: "baize"
      description: "Baize API or MCP connector"
      transport: "stdio"
policy:
  allow_implicit_invocation: true
